# CXXFLAGS = -g -O0
# PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) # old version
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) -lgsl -lgslcblas -lm
VALGRIND_OPTS = --tool=memcheck --leak-check=full --dsymutil=yes